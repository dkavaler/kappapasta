var config = {};

config.tmi = {
    options: {
        options: {
            debug: true
        },
        connection: {
            random: "chat",
            reconnect: true
        }
    }
};

module.exports = config;
